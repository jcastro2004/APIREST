#Nos dice que si la tabla no existe que la cree y si existe que la actualice de forma automatica
#spring.jpa.hibernate.ddl-auto=update
#spring.application.name=apirestJava
#El puerto que va escuchar
#server.port=8081

#Permite imporar archivos de cofniguracion de variables de entorno, al ser opcionar si no se 
#encuentre la aplicacion se seguira ejecutando fin fallas
#spring.config.import=optional:file:.env[.properties]
# Configuracion de base de datos
# Variables de entorno
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.application.name=apirest
server.port=8080
spring.config.import=optional:file:.env[.properties]
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.profiles.active=dev

spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}